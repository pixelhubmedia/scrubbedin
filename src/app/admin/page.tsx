import PlaceholderPage from "@/components/ui/PlaceholderPage";

export default function AdminPage() {
  return (
    <PlaceholderPage
      title="Admin Dashboard"
      description="Platform administration tools: review hospital submissions, moderate reports, manage appeals, and view audit logs. Restricted to platform administrators."
      badge="Admin"
    />
  );
}
